---
description: This metadata description is deliberately identical in every document and must never be compared
---
# Duplicate-rule fixture

ALWAYS RUN THE COMPLETE VALIDATION SUITE BEFORE REQUESTING REVIEW FROM ANOTHER MAINTAINER!
Keep repository documentation synchronized whenever a public command changes for users today

# This very long heading is duplicated elsewhere but headings are never prose candidates

```md
Keep repository documentation synchronized whenever a public command changes for users
```

Change production database structures only after reviewing every migration carefully

aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
