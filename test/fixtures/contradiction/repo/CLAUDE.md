# Contradiction fixture

Never use TypeScript strict mode for any production package
Avoid signed release artifacts during deployment verification
Don't run database migrations after backup verification completes

Never delete snapshots after failed visual checks
Use encrypted customer exports for external transfer
Keep TypeScript strict mode documented for production package builds
Always use signed release artifacts, but never publish signed release artifacts externally
Always use signed release artifacts. Never publish signed release artifacts externally

```text
Avoid signed release artifacts during deployment verification
```
