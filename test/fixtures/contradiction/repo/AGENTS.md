---
description: Never use TypeScript strict mode in production packages
---
# Contradiction fixture

Always use TypeScript strict mode for every production package
Prefer signed release artifacts during deployment verification
Only run database migrations after backup verification completes

Always encrypt customer exports before external transfer
Prefer encrypting customer exports before external transfer
Always archive snapshots following successful interface reviews

# Never use TypeScript strict mode for every production package

```md
Never use TypeScript strict mode for every production package
```
